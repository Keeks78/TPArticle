{% extends 'base.html.twig' %}

{% block title %}
    {{article.title}}
{% endblock %}

{% block body %}

<div class="ad-title" style="background-image: url({{ article.coverImage}})">
    <div class="container">
        <h1>{{article.title}}</h1>
        <p>{{article.introduction}}</p>
        <p class="h2">
            <strong>{{article.rooms}} chambres</strong> pour <strong>{{article.price}}&euros;</strong> par nuit
        </p>
        <a href="#" class="btn btn-primary">Réserver !</a>
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-md-8 mb-3">
            {{ article.content | raw }}

            <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                <ol class="carousel-indicators">
                    {% for image in ad.images %}
                    <li data-target="#carouselExampleIndicators" data-slide-to="0" {% if loop.first %}class="{{ loop.index0 }}"{% endif %}></li>
                    {% endfor %}
                </ol>
                <div class="carousel-inner">
                    {% for image in ad.images %}
                    <div class="carousel-item {% if loop.first %}active{% endif %}">
                        <img class="d-block w-100" src="{{ image.url }}" alt="First slide">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>{{ article.title }}</h5>
                            <p>{{ image.caption }}</p>
                        </div>
                    </div>
                    {% endfor %}
                </div>
                <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        </div>
        <div class="col">
            <div class="row">
                <div class="col-4">
                    <img src="https://placehold.it/64x64" alt="Avatar de Paul">
                </div>
                <div class="col">
                    <h3>Paul Grandvaux</h3>
                    <span class="badge-primary">3 annonces</span>
                </div>
            </div>
            <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Consequuntur impedit ab porro tenetur itaque quod reiciendis harum repellendus voluptate perspiciatis!</p>
            <p>Impedit modi autem praesentium a quibusdam quam vel ex natus tenetur, quos molestiae eum assumenda! Doloribus inventore perspiciatis eveniet libero?</p>
            <p>Pariatur quia praesentium, atque earum cum aliquam neque ut voluptates. Corrupti perferendis harum illum minima mollitia, adipisci quae debitis fugiat.</p>
        </div>
    </div>
</div>

{% endblock %}
